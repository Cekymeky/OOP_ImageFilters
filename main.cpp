#include "BlackBox.cpp"

using namespace std;

#define fast ios_base::sync_with_stdio(false), cout.tie(0), cin.tie(0);

typedef long long ll;
typedef double long dl;
typedef unsigned long long ull;


int main(){

    // string filename = "photographer";
    // string resultName = "Black";

    // BlackBox bx;
    // bx.SetImagesPath("Images");
    // bx.SetSavedImagesPath("Saved_Images");


    // string filepath = bx.GetImagesPath();
    // string SavedPath = bx.GetSavedImagesPath();


    // Image_Processing imageproc(filepath, SavedPath);

    // ImagePtr image = imageproc.LoadImage(filename);

    // imageproc.BlackWhiteFilter(image);
    // imageproc.SaveImage(resultName, image);

    // free(image);

    return EXIT_SUCCESS;
}   
